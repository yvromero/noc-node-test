import { Server } from "./presentation/server";
import { envs } from './config/plugins/envs.plugins';


(async() => {
    main();
})();

function main(){

    // console.log( envs );
    // Server.start();
}